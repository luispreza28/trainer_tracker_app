# Android module

/.gradle/
.cxx/
captures/
local.properties

# Build outputs
**/build/

# Keystores / signing (keep private)
key.properties
**/*.keystore
**/*.jks

# DO commit the wrapper + scripts (so do NOT ignore them)
# gradle/wrapper/gradle-wrapper.jar
# gradle/wrapper/gradle-wrapper.properties
# gradlew
# gradlew.bat
