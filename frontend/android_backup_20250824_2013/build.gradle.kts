// Root build file — pins plugin versions for the modules.
// DO NOT apply these here; modules will apply them without versions.
plugins {
    id("com.android.application") version "8.11.0" apply false
}

// (Optional) you can keep this empty; module builds pick up versions from above.
